import React from 'react';
import { Heading } from '../styles';

export const Header = () => {
  return (
    <Heading gradient color={'blue'} size="3" css={{ width: 'fit-content' }}>
      Minhas tarefas
    </Heading>
  );
};
